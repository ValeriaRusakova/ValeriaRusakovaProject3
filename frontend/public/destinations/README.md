# Destination photos

Curated hero photos used by `CardMedia` when a vacation record has no uploaded
image in the database.

Files here are served by Vite at `/destinations/<filename>` and referenced
from `src/utils/destinationImages.ts`.

Current files:
- `tel-aviv.jpg` — Tel Aviv promenade at dusk
